<%# console.log("Item:", key, value) %>
<div class="row">
  <div class="col first-col">
    <%= key %>
  </div>
  <div class="col-auto ml-1 mt-1">
  <% if (typeof value === 'object') { %>
    <button type="button" 
      class="btn btn-secondary btn-sm object-property-button" 
      data-popover-id="<%= popoverID %>"
      data-key="<%= key %>">Show</button>  
    <div id="<%= popoverID %>" title="<%= key %>"></div>
  <% } else if(typeof value === 'boolean') { %>
    <input type="checkbox" class="form-check-input value-boolean" <% if(value) { %> checked <% } %>" /> 
  <% } else { %>
    <input type="text" value="<%= value %>" class="value-text" /> 
  <% } %>
  </div>
</div>
