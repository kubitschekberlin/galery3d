{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            //"program": "${workspaceFolder}/three_noserver.js",
            "preLaunchTask": "webpack" // Name der Aufgabe, die vor dem Start ausgeführt werden soll
        }
    ]
}